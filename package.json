{
  "name": "prep2meet",
  "version": "1.0.0",
  "private": true,
  "description": "Instant pre-meeting briefings for SaaS sellers",
  "scripts": {
    "dev": "npx vercel dev",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "pages:dev": "npx serve public -p 8080"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
